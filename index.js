const calculator = document.querySelector(".calculator-container");
const keys = calculator.querySelector(".buttons-container");

keys.addEventListener("click", e => {
  if (e.target.matches("div")) {
  }
});
